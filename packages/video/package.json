{
  "name": "@autopilot/video",
  "version": "1.0.0",
  "description": "FFmpeg processing, video generation adapters, and Live loop compiler",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@autopilot/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
